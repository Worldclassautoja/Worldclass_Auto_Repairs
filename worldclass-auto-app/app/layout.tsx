import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'], variable: '--font-inter' });

export const metadata: Metadata = {
  title: "WorldClass Auto Repairs — Jamaica's Premier Auto Service",
  description:
    'Professional automotive repairs and maintenance in Jamaica. Engine diagnostics, brake service, transmission, electrical, AC, tyre alignment and more.',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={inter.variable}>
      <body>{children}</body>
    </html>
  );
}
